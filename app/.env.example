# Banner Studio · 環境變數範本
#
# 部署到 Vercel 時複製到 Project Settings → Environment Variables
# 本地開發複製成 .env.local(不進 git)

# Google Chat Incoming Webhook(Kay 收即時通知)
# 取得方式:Google Chat → 群組 → 整合 → Webhook → 新增 → 複製 URL
CHAT_WEBHOOK=https://chat.googleapis.com/v1/spaces/XXXXX/messages?key=...&token=...

# Google Apps Script Web App URL(累積申請紀錄到 Sheet)
# 取得方式:Apps Script 編輯器 → 部署 → 新增部署 → Web app → 複製 URL
# Execute as: Me / Who has access: Anyone
SHEETS_WEBHOOK_URL=https://script.google.com/macros/s/XXXXX/exec
